<<<<<<< HEAD
clear, clc
%Script for data processing EEM data
%Created By: Marcos Lee
%Modified By: Ciara Lugo


%Importing data from EXCEL
file1 = input('What UV file would you like to import? Include ".txt"(macs)  ', 's');      %to get filename to import
file2 = input('What FL file would you like to import? Include ".txt"(macs)  ', 's');      %to get filename to import
=======
clear all, clc

%Script for processing EEM data
%Created By: Marcos Lee
%Modified By: 


%Importing data from EXCEL
file1 = input('What UV file would you like to import? Include ".xlsx"  ', 's');      %to get filename to import
file2 = input('What FL file would you like to import? Include ".xlsx"  ', 's');      %to get filename to import
>>>>>>> 27724ad61bad31e84e21b104f89a109b725462c2

UV= xlsread(file1); %Converts data into Matrix
EEM= xlsread(file2); %Converts data into Matrix

<<<<<<< HEAD
%[rows, colms] = size();
=======
% [rows, colms] = size(UV);

>>>>>>> 27724ad61bad31e84e21b104f89a109b725462c2


for i = 1 : 3
    EEM(i,:)=[];
end 
EEM(1,:)=[];

AbdOD=UV(:,10); %defining necessary vectors 
UVnums=EEM(:,1); %defining necessary vectors 
WVLNG=UV(:,1); %defining necessary vectors 
AbdOD=flip(AbdOD); %flips order of vector to match order of FL data 
WVLNG=flip(WVLNG); %flips order of vector to match order of FL data

<<<<<<< HEAD
for i = 1: length(WVLNG) %should delete every wave length and abs associated with that wave length except for the one right below
                % the needed (first wave length value in FL xlsx) value but does not HELP!!! 
                % need that value for linear interpolation. 
    if(i >1)
    l = length(WVLNG);
    disp(l)
    if UVnums(1) > WVLNG(i)
        n = i-1;
        WVLNG(n)=[];
        AbdOD(n)=[];
        fprintf("i == %i.\n", i) 
        fprintf("WVLNG(i) == %f.\n", WVLNG(i))
        fprintf("UVnums(1) ==  %f.\n", UVnums(1))
        %disp(WVLNG(i))
        %disp(AbdOD(i))
    elseif(i >= l)
        break
    end 
    disp(i)
end

        
        
=======
for i= 2: length(WVLNG) %should delete every wave length and abs associated with that wave length except for the one right below
                % the needed (first wave length value in FL xlsx) value but does not HELP!!! 
                % need that value for linear interpolation. 
            
    if UVnums(1) > WVLNG(i)
        WVLNG(i-1)=[NaN];
        AbdOD(i-1)=[NaN];
    else
        break
    end 
    
end

dN=isnan(WVLNG);

%{
for i = 1:length(WVLNG)
    if dN == 1 
        WVLNG(i)=[]
        AbdOD(i)=[];
        dN(i)=[]
        dN2(i)=[];
    else 
        break 
    end 
end
        
%}
WVLNG(dN)=[];
AbdOD(dN)=[];
Ab=zeros(length(UVnums))
for i = length(UVnums)
    
>>>>>>> 27724ad61bad31e84e21b104f89a109b725462c2
        
